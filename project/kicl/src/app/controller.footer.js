(
    function (app) {
        'use strict';
        
        app
            .controller('footer',
                [
                    '$rootScope', '$scope', '$timeout', '$element', 'tween',
                    function (root, scope, timeout, elm, tween) {
                        
                    }
                ]
            );
    }
)(kicl);
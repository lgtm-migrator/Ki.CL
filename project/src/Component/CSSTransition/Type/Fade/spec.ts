import CSSTransition from '@/Component/CSSTransition/spec';

declare module Spec {
  type ClassName = CSSTransition.ClassName;
  type Props = CSSTransition.Props;
  type Type = CSSTransition.Type;
}

export default Spec;
